import {isEmptyObject} from '../../helper/functions';
import {

} from '../types';

const initialState = {

};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

